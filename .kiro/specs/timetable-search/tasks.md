# 時刻表検索機能 - 実装タスク

- [x] 1. Git運用の準備
  - developブランチを作成してチェックアウト
  - .gitignoreを更新（node_modules, .DS_Store, .env等を除外）
  - _Requirements: 該当なし（開発フロー）_

- [x] 2. プロジェクト初期設定
  - package.jsonを作成し、必要な開発依存関係をインストール（eslint, prettier, vitest, playwright, http-server）
  - .eslintrc.json、.prettierrc.json、vitest.config.js、playwright.config.jsを作成
  - 変更をdevelopブランチにコミット
  - _Requirements: 8.1, 8.2_

- [x] 3. HTMLとCSSの基本構造を実装
  - index.htmlを作成し、基本的なHTML構造を実装（head、body、検索フォーム、結果表示エリア）
  - favicon、PWA用アイコンのlinkタグを追加（アイコンファイルは後で追加）
  - app.cssを作成し、Mobile Firstのレスポンシブデザインを実装（ブレークポイント: 768px, 1024px）
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 4. データローダーモジュールを実装
  - data-loader.jsを作成し、DataLoaderクラスを実装
  - CSVファイルを並列fetchで読み込む機能を実装（bus_stop.csv, timetable_all_complete.csv, fare_major_routes.csv）
  - CSVをパースしてJavaScriptオブジェクトに変換する機能を実装
  - データをメモリにキャッシュする機能を実装
  - エラーハンドリングを実装（fetch失敗、parse失敗、タイムアウト）
  - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5_

- [x] 4.1 データローダーの単体テストを作成
  - tests/data-loader.test.jsを作成
  - CSVパース機能のテストを実装
  - データ読み込み機能のテストを実装（モックデータ使用）
  - _Requirements: 8.1, 8.2, 8.3_

- [x] 5. 時刻ユーティリティモジュールを実装
  - utils.jsを作成し、TimeUtilsクラスを実装
  - NTPサーバー（ntp.nict.go.jp）から現在時刻を取得する機能を実装（タイムアウト5秒）
  - NTP失敗時のフォールバック（ローカル時刻使用）を実装
  - 祝日カレンダーAPI（holidays-jp.github.io）から祝日データを取得する機能を実装（年ごとにキャッシュ）
  - 曜日・祝日判定機能を実装（月〜金かつ非祝日→平日、土日または祝日→土日祝）
  - 時刻フォーマット機能を実装（HH:MM形式）
  - 所要時間計算機能を実装
  - _Requirements: 2.2, 2.3, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 5.1 時刻ユーティリティの単体テストを作成
  - tests/utils.test.jsを作成
  - 曜日・祝日判定のテストを実装
  - 時刻フォーマットのテストを実装
  - 所要時間計算のテストを実装
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 6. バス停検索UI（インクリメンタルサーチ）を実装
  - app.jsにUIControllerクラスを作成
  - 乗車バス停入力フィールドにインクリメンタルサーチを実装（1文字以上で候補表示）
  - 降車バス停入力フィールドにインクリメンタルサーチを実装
  - バス停候補リストの表示・選択機能を実装
  - 同一バス停選択時のエラー表示を実装
  - textContentを使用してXSS対策を実装
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 7.1, 7.2, 7.4_

- [x] 7. 時刻選択UIを実装
  - 5つの検索オプション（出発時刻指定、到着時刻指定、今すぐ、始発、終電）のラジオボタンを実装
  - 時刻指定選択時のタイムピッカー（24時間形式）を実装
  - 「今すぐ」選択時にNTPから現在時刻を取得する機能を実装
  - NTP失敗時の警告メッセージ表示を実装
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7, 2.8, 7.4_

- [x] 8. 検索コントローラーを実装
- [ ] 
  - app.jsにSearchControllerクラスを作成
  - 直通便検索アルゴリズムを実装（乗車バス停が降車バス停より前に出現するtripをフィルタ）
  - 曜日区分によるフィルタリングを実装
  - 時刻によるフィルタリングを実装（出発時刻指定、到着時刻指定、今すぐ、始発、終電）
  - 所要時間計算機能を実装
  - 運賃取得機能を実装（fare_major_routes.csvから検索）
  - 検索結果のソート機能を実装（出発時刻昇順または到着時刻降順）
  - 検索結果を最大20件に制限する機能を実装
  - 2秒以内に検索完了するよう最適化
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_

- [x] 8.1 検索アルゴリズムの単体テストを作成
  - tests/search.test.jsを作成
  - 直通便フィルタリングのテストを実装
  - 時刻フィルタリングのテストを実装
  - 所要時間計算のテストを実装
  - 運賃取得のテストを実装
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_

- [x] 9. 検索結果表示UIを実装
  - 検索結果リストのHTML生成機能を実装（出発時刻、到着時刻、所要時間、運賃、事業者、路線名）
  - 検索結果0件時のメッセージ表示を実装
  - 検索結果20件超過時の「もっと見る」ボタンを実装
  - ローディング表示を実装
  - エラーメッセージ表示を実装
  - textContentを使用してXSS対策を実装
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 6.1, 6.2, 6.3, 6.4, 6.5_

- [x] 10. イベントハンドラーを実装
  - 検索ボタンクリック時のイベントハンドラーを実装
  - バス停選択時のイベントハンドラーを実装
  - 時刻オプション変更時のイベントハンドラーを実装
  - 入力検証を実装（乗車バス停未選択、降車バス停未選択、同一バス停選択）
  - エラーメッセージのクリア機能を実装
  - _Requirements: 1.3, 1.4, 2.1, 7.1, 7.2, 7.5_

- [x] 11. アプリ初期化処理を実装
  - DOMContentLoaded時にデータローダーを実行
  - データ読み込み完了後にUIを有効化
  - データ読み込み失敗時のエラー表示とリトライボタンを実装
  - 3秒以内にデータ読み込み完了を確認
  - _Requirements: 8.1, 8.2, 8.3, 8.4, 8.5, 7.3_

- [x] 12. セキュリティ対策を実装
  - _headersファイルを作成し、CSP、X-Frame-Options、X-Content-Type-Options等のセキュリティヘッダーを設定
  - 全てのDOM操作でtextContentまたはcreateElementを使用（innerHTML禁止）
  - バス停名の入力検証を実装（既存リストに存在するかチェック）
  - 時刻の入力検証を実装（0-23時、0-59分）
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 7.1, 7.2_

- [x] 13. レスポンシブデザインを実装
  - スマートフォン用レイアウトを実装（縦スクロール、フルスクリーン）
  - タブレット用レイアウトを実装（2カラム、768px以上）
  - デスクトップ用レイアウトを実装（3カラム、1024px以上）
  - タップターゲットを最小44x44pxに設定
  - ビューポート設定を追加（maximum-scale=5.0）
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 14. E2Eテストを作成
  - e2e/search.spec.jsを作成
  - バス停選択フローのテストを実装
  - 検索実行フローのテストを実装
  - 検索結果表示確認のテストを実装
  - エラーハンドリング確認のテストを実装
  - レスポンシブデザインのテストを実装（375px、768px、1920px）
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.1, 4.1, 5.1, 7.1, 7.2_

- [x] 15. アイコンを生成
  - AI画像生成ツール（DALL-E、Midjourney等）で512x512pxのマスターアイコンを生成
  - ImageMagickで各サイズのアイコンを生成（192px、180px、32px、16px）
  - favicon.icoを生成（16x16、32x32、48x48を含む）
  - icons/ディレクトリに全てのアイコンを配置
  - _Requirements: 該当なし（デザイン要件）_

- [x] 16. manifest.jsonを作成（PWA準備）
  - manifest.jsonを作成し、アプリ名、アイコン、テーマカラー等を設定
  - index.htmlにmanifest.jsonのlinkタグを追加
  - _Requirements: 該当なし（Phase 5で本格実装）_

- [x] 17. 手動テストを実施
  - 正常系テスト（佐賀駅バスセンター→佐賀大学、県庁前、西与賀）を実施
  - 異常系テスト（未選択、同一バス停、該当便なし）を実施
  - 境界値テスト（最初の便、最後の便、20件超過）を実施
  - パフォーマンステスト（データ読み込み3秒以内、検索2秒以内）を実施
  - ブラウザ互換性テスト（Chrome、Safari、Firefox、Edge）を実施
  - レスポンシブテスト（iPhone SE、iPhone 14 Pro、iPad、Desktop）を実施
  - _Requirements: 全要件_

- [-] 18. developブランチをpushしてPR作成
  - 全ての変更をdevelopブランチにコミット
  - developブランチをGitHubにpush
  - GitHubでdevelop → mainのPull Requestを作成
  - PRの説明に実装内容とテスト結果を記載
  - _Requirements: 該当なし（開発フロー）_

- [x] 19. Cloudflare Pagesにデプロイ
  - PRをマージしてmainブランチに反映
  - Cloudflare Pagesプロジェクトを作成
  - GitHubリポジトリ（mainブランチ）と連携
  - カスタムドメイン（saga-bus.midnight480.com）を設定
  - 自動デプロイを有効化
  - 本番環境で動作確認
  - _Requirements: 全要件_
